// reinforce the concept that in a loop (for or while)
// we can put calls to functions in the execution context
//let's do an example:

function a(){ return 0; }
function b(){ }
function keepGoing(){return true; }

//for ( a() ;  keepGoing() ; b() ){
//    console.log("yeahh! feeling happy!!");
//}
let counter = 0;
function peanut(){
    return counter++;
    if (counter > 10){return false;}
    return true;
}

while(peanut()){
    
}
