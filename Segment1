
// 100 down to 5; step down by 7
// sum up the numbers we step over
sum = 0;
startNumber = 100;
endNumber = 5;
stepNumber = -7; 
currentNumber = startNumber;
for ( ; currentNumber < 5 ; ){
    currentNumber+= stepNumber;
    sum += currentNumber;
}
console.log("sum is ", sum);
